# Local Prometheus config (no Docker)
# Scrapes the FastAPI app running on localhost:8000 at /metrics/prometheus
global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "flashpoint"
    metrics_path: /metrics/prometheus
    static_configs:
      - targets: ["localhost:8000"]
